<% if flash[:notice] %>
    <%= flash[:notice] %>
<% end %>

<h1>一覧</h1>

<% @users.each do |u| %>
  <p><%= u.uid %></p>
  <%= link_to '削除', user_path(u), data: {turbo_method: :delete} %>
<% end %>

<p><%= link_to '新規登録', new_user_path %></p>
<p><%= link_to 'ログイン', root_path %></p>